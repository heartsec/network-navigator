# 动态加载功能 - 快速开始

我已经为你创建了一个完整的动态加载解决方案，无需修改原始文件。

## 📁 创建的文件

1. **LoadNetworkDynamic.js** - 新的动态加载组件
2. **examples-list.json** - 示例文件配置
3. **启用/恢复脚本** - 快速切换工具

## 🚀 如何使用

### 方法一：手动启用（推荐）

编辑 `src/components/App.js`，修改第 5 行：

```javascript
// 原来的
import LoadNetwork from "./LoadNetwork";

// 改为
import LoadNetwork from "./LoadNetworkDynamic";
```

保存后页面会自动刷新。

### 方法二：使用脚本启用

```bash
cd /Users/heartsec-macmini/Projects/network-navigator
./scripts/enable-dynamic-loading.sh
```

## ➕ 添加新示例

### 步骤 1: 复制文件到 public 目录
```bash
cp your_file.ftree public/
```

### 步骤 2: 更新配置文件

编辑 `public/examples-list.json`：

```json
{
  "files": [
    {
      "filename": "citation_data.ftree",
      "name": "Citation Network",
      "description": "Citation network data example"
    },
    {
      "filename": "tiny_fact_graph_valid.ftree",
      "name": "Tiny Fact Graph",
      "description": "Small fact graph for testing"
    },
    {
      "filename": "your_file.ftree",
      "name": "你的示例名称",
      "description": "示例描述"
    }
  ]
}
```

### 步骤 3: 刷新浏览器

新的示例会自动出现在下拉列表中！

## 🔄 恢复原始版本

### 方法一：手动恢复
编辑 `src/components/App.js`，改回：
```javascript
import LoadNetwork from "./LoadNetwork";
```

### 方法二：使用脚本
```bash
./scripts/restore-original-loader.sh
```

## ✨ 特性

- ✅ **完全动态** - 只需编辑 JSON 配置，无需修改代码
- ✅ **不破坏原有代码** - 原始组件完整保留
- ✅ **自动格式化** - 文件名自动转换为可读标题
- ✅ **降级处理** - 配置文件缺失时使用默认列表
- ✅ **易于扩展** - 添加新示例只需 3 步

## 📋 示例：添加多个文件

```json
{
  "files": [
    {
      "filename": "network1.ftree",
      "name": "社交网络",
      "description": "社交网络分析"
    },
    {
      "filename": "network2.ftree",
      "name": "生物网络",
      "description": "蛋白质相互作用网络"
    },
    {
      "filename": "network3.ftree",
      "name": "交通网络",
      "description": "城市交通流量分析"
    }
  ]
}
```

## 🐛 故障排除

### 问题：新文件不显示
- 检查文件是否在 `public/` 目录
- 检查 `examples-list.json` 语法是否正确
- 刷新浏览器缓存 (Cmd+Shift+R)

### 问题：加载失败
- 确认文件名在配置中拼写正确
- 检查 `.ftree` 文件格式是否有效
- 查看浏览器控制台错误信息

## 📝 配置文件字段说明

```json
{
  "files": [
    {
      "filename": "文件名.ftree",        // 必需：public 目录下的实际文件名
      "name": "显示名称",                // 必需：在下拉列表中显示的名称
      "description": "描述文字"          // 可选：显示在下拉列表中的描述
    }
  ]
}
```

---

现在你可以轻松管理所有示例文件了！🎉
